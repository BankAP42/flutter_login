import 'package:flutter/material.dart';

// For all active and normal commonent or other
const primaryBlue = Color.fromRGBO(0, 179, 239, 1);

const accentGrey = Color.fromRGBO(107, 107, 107, 1);

const blackPrimary = Colors.black;
const bgColor = Colors.white;
